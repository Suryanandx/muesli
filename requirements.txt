# Web
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
jinja2>=3.1.0

# AI
faster-whisper>=1.0.0
anthropic>=0.40.0
openai>=1.50.0

# Audio
sounddevice>=0.5.0
numpy>=1.26.0

# Database
asyncpg>=0.29.0          # PostgreSQL (used when DATABASE_URL is set)
aiosqlite>=0.20.0        # SQLite fallback (local dev without Docker)

# Cache
redis[hiredis]>=5.0.0

# Config
python-dotenv>=1.0.0
